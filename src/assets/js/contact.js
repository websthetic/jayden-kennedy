import { initForms } from "./netlifyforms.js";

/**
 * Contact page.
 *
 * No reveal here — the page is one screen of form and the animation would
 * fire before anyone had scrolled.
 */

initForms();